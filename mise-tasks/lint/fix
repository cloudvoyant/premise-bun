#!/usr/bin/env bash
#MISE description="Auto-fix ESLint issues (does not fix type errors)"
#MISE depends=["install"]
set -euo pipefail
cd "${MISE_PROJECT_ROOT:-$PWD}/packages/premise-bun"
bun run lint:fix
